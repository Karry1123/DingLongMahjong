# 修改日志

## v0.1.1-beta — 2026-09-24

### 修复

- **副露步进与供牌绑定**：修复明杠后先摸岭上牌导致历史栈末条变为杠后快照的问题。吃、碰、明杠现在使用动作开始时保存的完整局面提交 `/api/game/step`；暗杠及对手杠牌同步也固定使用各自的动作前快照。
- **牌河扣减**：副露事件统一使用绝对风位 `E/S/W/N` 指明供牌者，并携带该响应牌在供牌者牌河中的原始位置。后端优先按此位置校验并扣牌；没有位置时仍要求牌河末张匹配，避免误取其他座位或同名历史弃牌。明杠补牌后保留唯一一次岭上摸牌，并正确进入自家切牌阶段。
- **异常反馈**：PvE 模式遇到供牌位置校验失败时保留已完成的本地副露和全桌局面，收起底层错误红字；后续步进请求继续携带完整局面。步进接口为无状态接口，没有可读取的服务端会话快照。
- **副露 EV**：将幺九明杠 16 胡、中张明杠 8 胡纳入响应评估，并为暗刻接第 4 张牌建立相对碰牌的胡数与岭上补张价值下界，修复明杠低于碰牌的错误排序。
- **移动端语音**：开始 PvE 时通过用户点击预热 Web Speech API；异步等待中文语音列表加载。设备没有可用语音或播报失败时，播放短提示音。

### 界面与体验

- 自家操作工作台按**手牌 → 副露 → 牌河**排列。席位与积分标题横跨工作台两列，使手牌和右侧 EV 推荐从同一高度开始。
- 手机竖屏时将 PvE 页面旋转 90 度并压缩牌桌与手牌尺寸；牌桌、手牌和推荐面板保留在一个视口中，页面不产生横向或纵向滚动。
- 页面底部显示直接来自前端包版本号的 `v0.1.1-beta`。

### 验证

- 后端副露决策与步进接口测试：28 项通过；包含 W 打出九条后自家明杠、供牌位置错位及来源校验。
- 前端测试：40 项通过；包含供牌方历史快照、失败后本地状态保留与移动端语音回退。
- 本地 FastAPI 联调：W 打出 `9s` → 自家明杠 → 步进接口 200 → W 牌河扣张 → 岭上补牌一次 → 进入可切牌状态。
- Chromium PvE 冒烟测试：工作台顺序、EV 面板对齐及 390×844 竖屏视口检查通过；生产构建通过。
